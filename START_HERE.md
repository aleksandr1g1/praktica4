# 🚀 НАЧНИТЕ ЗДЕСЬ - Быстрая установка

## Простая установка (один скрипт)

Откройте терминал и выполните:

```bash
cd /home/lovesosa/praktica
./install.sh
```

Скрипт автоматически:
1. ✅ Установит Node.js и npm
2. ✅ Установит PostgreSQL
3. ✅ Создаст базу данных
4. ✅ Установит все зависимости
5. ✅ Создаст администратора и психолога

**Вам потребуется ввести пароль администратора несколько раз.**

---

## Если возникли проблемы с доступом к PostgreSQL

Выполните дополнительный скрипт:

```bash
cd /home/lovesosa/praktica
./setup-postgresql.sh
```

---

## Запуск приложения

После успешной установки:

```bash
cd /home/lovesosa/praktica
npm run dev
```

Откройте браузер: **http://localhost:3000**

---

## Тестовые аккаунты

После установки будут доступны:

- 👤 **Администратор**
  - Email: `admin@test.com`
  - Пароль: `admin123`

- 👨‍⚕️ **Психолог**
  - Email: `psychologist@test.com`
  - Пароль: `psych123`

- 👥 **Обычные пользователи**
  - Создаются через регистрацию на сайте

---

## Структура базы данных

База данных `psychological_tests` содержит:

### 1. **users** - Пользователи
- id (UUID)
- email, username, password
- role (user, psychologist, admin)
- firstName, lastName
- isActive
- createdAt, updatedAt

### 2. **tests** - Материалы тестов
- id (UUID)
- name (реальное название, видно психологу/админу)
- displayName (отображаемое название для пользователей)
- description
- methodicalRecommendations
- totalQuestions
- timeLimit
- isActive
- createdAt, updatedAt

### 3. **questions** - Вопросы тестов
- id (UUID)
- testId (FK -> tests)
- questionNumber
- imagePath (путь к изображению)
- questionText
- numberOfOptions (6-8)
- correctAnswer
- createdAt, updatedAt

### 4. **test_results** - Результаты тестирования
- id (UUID)
- userId (FK -> users, nullable для гостей)
- testId (FK -> tests)
- status (in_progress, completed, abandoned)
- score (правильных ответов)
- totalQuestions
- percentage
- isSaved (сохранён ли результат)
- timeSpent (секунды)
- interpretation (jsonb)
- createdAt, updatedAt, completedAt

### 5. **answers** - Ответы на вопросы
- id (UUID)
- testResultId (FK -> test_results)
- questionId (FK -> questions)
- selectedAnswer
- isCorrect
- createdAt

---

## Проверка установки

### Проверить Node.js:
```bash
node --version
npm --version
```

### Проверить PostgreSQL:
```bash
sudo systemctl status postgresql
```

### Войти в базу данных:
```bash
psql -U postgres -d psychological_tests
```

В psql:
```sql
\dt              -- показать все таблицы
\d users         -- структура таблицы users
SELECT * FROM users;  -- посмотреть пользователей
\q               -- выйти
```

---

## Что делать, если что-то не работает?

### 1. PostgreSQL не запускается
```bash
sudo systemctl status postgresql
sudo journalctl -u postgresql -n 50
```

### 2. Ошибка подключения к БД
```bash
# Проверьте, что PostgreSQL запущен
sudo systemctl start postgresql

# Проверьте настройки в .env файле
cat backend/.env
```

### 3. Не установлены зависимости
```bash
cd backend
npm install

cd ../frontend
npm install
```

### 4. Порт занят
Если порт 3000 или 5000 занят, измените в конфигурации:
- Frontend: `frontend/vite.config.ts` (измените port)
- Backend: `backend/.env` (измените PORT)

---

## Дополнительная документация

- 📖 **README.md** - Полная документация проекта
- 📋 **INSTALLATION.md** - Подробная инструкция по установке
- ⚡ **QUICKSTART.md** - Руководство по быстрому старту
- 🐳 **DOCKER.md** - Запуск через Docker
- 📁 **PROJECT_STRUCTURE.md** - Структура проекта

---

## Поддержка

Если возникли вопросы, проверьте:
1. Логи в терминале при запуске
2. Консоль браузера (F12)
3. Файлы документации в папке проекта

---

**Готово! Теперь запустите `./install.sh` и начните работу! 🎉**


