# Быстрый старт

Самый простой способ запустить систему психологического тестирования.

## За 5 минут

### 1. Установите PostgreSQL (если еще не установлен)

**Fedora:**
```bash
sudo dnf install postgresql postgresql-server
sudo postgresql-setup --initdb
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install postgresql
```

### 2. Создайте базу данных

```bash
sudo -u postgres psql
```

В psql выполните:
```sql
CREATE DATABASE psychological_tests;
\q
```

### 3. Установите зависимости

```bash
cd /home/lovesosa/praktica
npm run install:all
```

### 4. Создайте админа и психолога

```bash
cd backend
npx ts-node src/scripts/create-admin.ts
cd ..
```

Будут созданы:
- 👤 **Админ**: admin@test.com / admin123
- 👨‍⚕️ **Психолог**: psychologist@test.com / psych123

### 5. Запустите приложение

```bash
npm run dev
```

### 6. Откройте в браузере

🌐 http://localhost:3000

---

## Первые шаги

### Как администратор (admin@test.com / admin123):

1. Войдите в систему
2. Перейдите в **Админ-панель**
3. Создайте новый тест во вкладке **"Создать тест"**:
   - Название: "Прогрессивные матрицы Равена"
   - Отображаемое название: "Тест 1"
   - Описание: "Тест для оценки невербального интеллекта"
   - Методические рекомендации: "Инструкции для психолога"
4. Добавьте вопросы с изображениями

### Как пользователь:

1. Нажмите **"Регистрация"**
2. Создайте аккаунт
3. Перейдите в **"Тесты"**
4. Выберите тест и пройдите его
5. Решите, сохранять результат или нет

### Как психолог (psychologist@test.com / psych123):

1. Войдите в систему
2. Перейдите в **"Статистика"**
3. Просматривайте результаты всех пользователей
4. Анализируйте статистику по тестам

---

## Структура проекта

```
praktica/
├── backend/          # Node.js + Express + TypeScript
├── frontend/         # React + TypeScript
├── README.md         # Полная документация
├── INSTALLATION.md   # Подробная инструкция
├── DOCKER.md         # Запуск через Docker
└── QUICKSTART.md     # Этот файл
```

---

## Полезные команды

```bash
# Запуск всего проекта
npm run dev

# Только backend
cd backend && npm run dev

# Только frontend
cd frontend && npm run dev

# Создать админа
cd backend && npx ts-node src/scripts/create-admin.ts

# Подключиться к БД
sudo -u postgres psql psychological_tests
```

---

## Возникли проблемы?

1. Проверьте, что PostgreSQL запущен:
   ```bash
   sudo systemctl status postgresql
   ```

2. Проверьте порты (должны быть свободны):
   - 3000 (frontend)
   - 5000 (backend)
   - 5432 (PostgreSQL)

3. Проверьте логи в терминале

4. Читайте подробную документацию в `README.md` и `INSTALLATION.md`

---

## Что дальше?

- Прочитайте полную документацию в `README.md`
- Изучите API endpoints
- Настройте для production (см. `README.md`)
- Попробуйте запустить через Docker (см. `DOCKER.md`)

---

**Готово! 🎉** Теперь у вас работает полноценная система психологического тестирования!


