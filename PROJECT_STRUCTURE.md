# Структура проекта

Полное описание структуры системы психологического тестирования.

## 📁 Корневая директория

```
praktica/
├── backend/                  # Backend приложение
├── frontend/                 # Frontend приложение
├── .gitignore               # Git игнорируемые файлы
├── package.json             # Корневой package.json
├── README.md                # Основная документация
├── INSTALLATION.md          # Подробная инструкция по установке
├── QUICKSTART.md            # Быстрый старт
├── DOCKER.md                # Docker инструкции
├── PROJECT_STRUCTURE.md     # Этот файл
├── database-setup.sql       # SQL скрипт для инициализации БД
└── docker-compose.yml       # Docker Compose конфигурация
```

## 🔧 Backend (Node.js + Express + TypeScript)

```
backend/
├── src/
│   ├── config/
│   │   └── database.ts           # Конфигурация TypeORM и подключение к БД
│   │
│   ├── entities/                 # Модели базы данных (TypeORM)
│   │   ├── User.ts              # Пользователи (user, psychologist, admin)
│   │   ├── Test.ts              # Тесты
│   │   ├── Question.ts          # Вопросы тестов
│   │   ├── TestResult.ts        # Результаты прохождения тестов
│   │   └── Answer.ts            # Ответы пользователей на вопросы
│   │
│   ├── controllers/             # Контроллеры (бизнес-логика)
│   │   ├── authController.ts    # Регистрация, вход, профиль
│   │   ├── testController.ts    # Тесты, прохождение, результаты
│   │   ├── adminController.ts   # Управление тестами и пользователями
│   │   └── psychologistController.ts  # Статистика и аналитика
│   │
│   ├── routes/                  # API роуты
│   │   ├── auth.routes.ts       # /api/auth/*
│   │   ├── test.routes.ts       # /api/tests/*
│   │   ├── admin.routes.ts      # /api/admin/*
│   │   └── psychologist.routes.ts  # /api/psychologist/*
│   │
│   ├── middleware/              # Middleware функции
│   │   └── auth.ts              # Аутентификация и авторизация по ролям
│   │
│   ├── utils/                   # Утилиты
│   │   ├── jwt.ts              # Генерация и проверка JWT токенов
│   │   └── fileUpload.ts       # Multer конфигурация для загрузки файлов
│   │
│   ├── scripts/                 # Утилитные скрипты
│   │   └── create-admin.ts     # Создание админа и психолога
│   │
│   └── index.ts                # Точка входа приложения
│
├── uploads/                    # Загруженные изображения вопросов
│   └── .gitkeep
├── .env                        # Переменные окружения
├── .env.example               # Пример переменных окружения
├── .gitignore                 # Git игнорируемые файлы
├── package.json               # Зависимости backend
├── tsconfig.json              # TypeScript конфигурация
└── Dockerfile                 # Docker образ для backend
```

### Основные зависимости Backend:
- **express** - веб-фреймворк
- **typeorm** - ORM для PostgreSQL
- **pg** - PostgreSQL драйвер
- **jsonwebtoken** - JWT токены
- **bcryptjs** - хеширование паролей
- **multer** - загрузка файлов
- **cors** - CORS middleware
- **helmet** - безопасность
- **morgan** - логирование

## 🎨 Frontend (React + TypeScript)

```
frontend/
├── src/
│   ├── api/                    # API клиенты
│   │   ├── axios.ts           # Axios конфигурация с interceptors
│   │   ├── authApi.ts         # API для аутентификации
│   │   ├── testApi.ts         # API для тестов
│   │   ├── adminApi.ts        # API для админа
│   │   └── psychologistApi.ts # API для психолога
│   │
│   ├── components/            # React компоненты
│   │   ├── Navbar.tsx        # Навигационная панель
│   │   ├── Navbar.css
│   │   └── ProtectedRoute.tsx # Защищённые роуты (по ролям)
│   │
│   ├── pages/                 # Страницы приложения
│   │   ├── Home.tsx          # Главная страница
│   │   ├── Home.css
│   │   ├── Login.tsx         # Страница входа
│   │   ├── Register.tsx      # Страница регистрации
│   │   ├── Auth.css          # Общие стили для auth страниц
│   │   ├── TestList.tsx      # Список тестов
│   │   ├── TestList.css
│   │   ├── TestTaking.tsx    # Прохождение теста
│   │   ├── TestTaking.css
│   │   ├── TestResult.tsx    # Результат теста
│   │   ├── TestResult.css
│   │   ├── UserResults.tsx   # Результаты пользователя
│   │   ├── UserResults.css
│   │   ├── Psychologist.tsx  # Страница психолога
│   │   ├── Psychologist.css
│   │   ├── Admin.tsx         # Админ-панель
│   │   └── Admin.css
│   │
│   ├── store/                # State management (Zustand)
│   │   └── authStore.ts      # Store для аутентификации
│   │
│   ├── types/                # TypeScript типы
│   │   └── index.ts         # Общие типы (User, Test, Question, etc.)
│   │
│   ├── App.tsx              # Главный компонент с роутингом
│   ├── main.tsx             # Точка входа React
│   ├── index.css            # Глобальные стили
│   └── vite-env.d.ts        # Vite типы
│
├── index.html               # HTML шаблон
├── .gitignore              # Git игнорируемые файлы
├── package.json            # Зависимости frontend
├── tsconfig.json           # TypeScript конфигурация
├── tsconfig.node.json      # TypeScript для Vite
├── vite.config.ts          # Vite конфигурация
├── nginx.conf              # Nginx конфигурация для Docker
└── Dockerfile              # Docker образ для frontend
```

### Основные зависимости Frontend:
- **react** - UI библиотека
- **react-router-dom** - роутинг
- **axios** - HTTP клиент
- **zustand** - управление состоянием
- **react-hot-toast** - уведомления
- **vite** - сборщик и dev server

## 🗄️ База данных (PostgreSQL)

### Таблицы:

1. **users** - Пользователи
   - id (UUID, PK)
   - email (unique)
   - username (unique)
   - password (hashed)
   - role (enum: user, psychologist, admin)
   - firstName, lastName
   - isActive
   - createdAt, updatedAt

2. **tests** - Тесты
   - id (UUID, PK)
   - name (реальное название)
   - displayName (для пользователей)
   - description
   - methodicalRecommendations
   - isActive
   - totalQuestions
   - timeLimit
   - createdAt, updatedAt

3. **questions** - Вопросы
   - id (UUID, PK)
   - testId (FK -> tests)
   - questionNumber
   - imagePath
   - questionText
   - numberOfOptions (6-8)
   - correctAnswer
   - createdAt, updatedAt

4. **test_results** - Результаты тестов
   - id (UUID, PK)
   - userId (FK -> users, nullable)
   - testId (FK -> tests)
   - status (enum: in_progress, completed, abandoned)
   - score (правильных ответов)
   - totalQuestions
   - percentage
   - isSaved (сохранён ли результат)
   - timeSpent (секунды)
   - interpretation (jsonb)
   - createdAt, updatedAt, completedAt

5. **answers** - Ответы на вопросы
   - id (UUID, PK)
   - testResultId (FK -> test_results)
   - questionId (FK -> questions)
   - selectedAnswer
   - isCorrect
   - createdAt

## 🔐 Роли и доступы

### User (обычный пользователь)
- ✅ Регистрация через интерфейс
- ✅ Прохождение тестов
- ✅ Просмотр своих результатов
- ❌ Не видит реальные названия тестов
- ❌ Нет доступа к статистике других

### Psychologist (психолог)
- ❌ Создаётся вручную в БД
- ✅ Просмотр всех результатов
- ✅ Статистика по тестам
- ✅ Статистика по пользователям
- ✅ Методические рекомендации
- ❌ Не может создавать тесты

### Admin (администратор)
- ❌ Создаётся вручную в БД
- ✅ Все права психолога
- ✅ Создание/редактирование тестов
- ✅ Добавление вопросов
- ✅ Управление пользователями
- ✅ Очистка результатов
- ✅ Полный доступ к системе

## 🚀 API Endpoints

### Auth
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/profile` - Профиль (auth)
- `PUT /api/auth/profile` - Обновить профиль (auth)

### Tests (публичные)
- `GET /api/tests` - Список тестов
- `GET /api/tests/:id` - Тест с вопросами
- `POST /api/tests/start` - Начать тест
- `POST /api/tests/answer` - Сохранить ответ
- `POST /api/tests/complete` - Завершить тест

### Results (user)
- `GET /api/results` - Мои результаты (auth)
- `GET /api/results/:id` - Детальный результат (auth)

### Psychologist (psychologist, admin)
- `GET /api/psychologist/results/all` - Все результаты
- `GET /api/psychologist/statistics/test/:testId` - Статистика теста
- `GET /api/psychologist/statistics/user/:userId` - Статистика пользователя
- `GET /api/psychologist/statistics/overall` - Общая статистика

### Admin (admin only)
- `POST /api/admin/tests` - Создать тест
- `PUT /api/admin/tests/:id` - Обновить тест
- `DELETE /api/admin/tests/:id` - Удалить тест
- `POST /api/admin/questions` - Добавить вопрос (multipart)
- `PUT /api/admin/questions/:id` - Обновить вопрос (multipart)
- `DELETE /api/admin/questions/:id` - Удалить вопрос
- `DELETE /api/admin/results/test/:testId` - Очистить результаты теста
- `DELETE /api/admin/results/all` - Очистить все результаты
- `GET /api/admin/users` - Список пользователей
- `PUT /api/admin/users/:id/toggle-status` - Активировать/деактивировать

## 📝 Переменные окружения

### Backend (.env)
```env
PORT=5000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=psychological_tests

JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=7d

UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880
```

## 🎯 Особенности реализации

### Безопасность
- JWT токены с истечением срока действия
- Bcrypt хеширование паролей (10 раундов)
- Защита роутов по ролям через middleware
- Helmet для HTTP заголовков безопасности
- CORS настроен корректно

### Валидация
- Express validator для входящих данных
- TypeScript типизация на frontend и backend
- TypeORM валидация на уровне БД

### Загрузка файлов
- Multer для обработки multipart/form-data
- Фильтрация типов файлов (только изображения)
- Ограничение размера файла (5MB)
- Автоматическое удаление при удалении вопроса

### Frontend особенности
- React Router v6 с защищёнными роутами
- Zustand для управления состоянием
- Axios interceptors для автоматической авторизации
- React Hot Toast для красивых уведомлений
- Responsive дизайн (mobile-friendly)

### Backend особенности
- TypeORM с автоматической синхронизацией схемы
- Async/await для всех операций с БД
- Централизованная обработка ошибок
- Morgan для логирования запросов
- Организация кода по роутам и контроллерам

## 🔄 Процесс тестирования

1. Пользователь выбирает тест
2. Backend создаёт запись TestResult со статусом `in_progress`
3. Для каждого вопроса создаётся Answer
4. При завершении:
   - Подсчитывается score (правильных ответов)
   - Вычисляется percentage
   - Статус меняется на `completed`
   - Пользователь решает сохранять результат
5. Если сохранён (`isSaved=true`), виден психологу

## 📊 Статистика

Психолог может видеть:
- Общее количество тестов, пользователей, результатов
- Статистику по каждому тесту (средний балл, процент, время)
- Статистику по каждому пользователю
- Детальные результаты с ответами на каждый вопрос
- Методические рекомендации по тестам

## 🐳 Docker

Проект включает:
- `docker-compose.yml` - оркестрация сервисов
- `backend/Dockerfile` - образ backend
- `frontend/Dockerfile` - multi-stage build для frontend
- `frontend/nginx.conf` - Nginx конфигурация

Запуск: `docker-compose up -d`

---

**Итог**: Полноценная система психологического тестирования с разделением ролей, статистикой и удобным интерфейсом! 🎉


